database:
  # sqlite is the default and stores data in the plugin data folder.
  type: sqlite
  sqlite:
    file: player-titles.db
  mysql:
    host: localhost
    port: 3306
    database: player_titles
    username: player_titles
    password: ""
    maximum-pool-size: 4
